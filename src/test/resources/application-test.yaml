spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/sensor_rest_app_db_test
    username: postgres
    password: PapaEmeritus4_
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true